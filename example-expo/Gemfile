# Isolated CocoaPods for the Expo example. Do not add the Square `refinement` gem
# here — it prepends Pod::Installer#perform_post_install_actions and conflicts with
# expo-modules-autolinking (stack level too deep during pod install).
source 'https://rubygems.org'

gem 'cocoapods', '1.16.2'
